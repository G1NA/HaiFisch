package com.haifisch.client;

/**
 * Interface for creating queries to be sent from a client to the server.
 */

public interface CreateQuery {
	
	/**
	 * Creates the query
	 */
	public void createQuery();
	
}
